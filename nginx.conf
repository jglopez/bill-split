server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve the SPA: unknown paths fall back to index.html so
    # client-side routing works after a hard refresh or direct link.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
